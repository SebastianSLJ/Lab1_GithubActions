name: main_workflow

on: 
    push: 
        branches: master


jobs: 
    call-reusable:
        uses: ./.github/workflows/reusable.yml
        with: 
            environment: "staging"
        secrets: inherit
